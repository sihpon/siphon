---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: siphons.my.domain
spec:
  group: my.domain
  names:
    kind: Siphon
    listKind: SiphonList
    plural: siphons
    singular: siphon
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Siphon is the Schema for the siphons API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SiphonSpec defines the desired state of Siphon.
            properties:
              foo:
                description: Foo is an example field of Siphon. Edit siphon_types.go
                  to remove/update
                type: string
              labels:
                additionalProperties:
                  type: string
                type: object
              versions:
                additionalProperties:
                  type: string
                type: object
            type: object
          status:
            description: SiphonStatus defines the observed state of Siphon.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
